name: master-beta

# Shipping master-tracking build of mpv-omniphony: mpv master + the orender
# spatial-audio patches, with native Dolby Vision Profile 7 FEL.
#
# As of 2026-07-01 FEL is UPSTREAM (mpv PR #17932 merged; libplacebo + ffmpeg
# carry it in their masters), so there is no fork/patch to track — this build is
# just "mpv master + orender". It still builds two deps from source because no
# release ships the FEL API yet: libplacebo master (PL_API_VER >= 370) and ffmpeg
# master (dovi_split BSF + DoVi stream group). See deps-fel/pins-fel.env and the
# "Dolby Vision FEL" section of README.md. scripts/build-fel-deps.sh builds them
# into a prefix; the mpv build links that prefix FIRST so it shadows the
# system/Martchus ffmpeg+libplacebo.
#
# Opt-in / prerelease channel: this does NOT touch integration-build.yml /
# release.yml (the stable v0.41.0 track).
on:
  workflow_dispatch:
  schedule:
    - cron: '0 7 * * 1' # weekly, Monday 07:00 UTC
  push:
    # Tag a beta (e.g. v0.4.0-fel-beta.1) to build AND publish a pre-release.
    # (The -fel tag namespace is the established prerelease channel; kept for
    # release-process continuity even though FEL is no longer a fork.)
    tags:
      - 'v*-fel*'
    paths:
      - 'patches-master/**'
      - 'deps-fel/**'
      - 'scripts/build-fel-deps.sh'
      - 'scripts/build-libbluray-bdj.sh'
      - 'scripts/build-libbluray-mingw.sh'
      - 'scripts/mpv-bdj-launcher-macos.sh'
      - 'scripts/MPV-BD菜单启动.bat'
      - 'scripts/BD_J_运行说明.txt'
      - 'scripts/apply-patches-master.sh'
      - '.github/workflows/build-master-beta.yml'

env:
  # Rolling (cron/dispatch/path-push) builds track Omniphony main; a tagged beta
  # pins liborender to the matching release line for reproducibility.
  OMNIPHONY_REF: ${{ startsWith(github.ref, 'refs/tags/') && 'v0.5.0' || 'main' }}
  # Java LTS used for BD-J (libbluray.jar compile + jlink runtime bundled on
  # macOS/Linux). Temurin 21 LTS ships on all runners. BDJ is tied to the
  # libbluray source build — it can't use distro/homebrew libbluray (those
  # ship bdj_jar=disabled by default).
  JAVA_VERSION: '21'
  JAVA_DISTRO: 'temurin'

jobs:
  # Resolve the live upstream libplacebo master HEAD once so every platform keys
  # its dep cache on the same SHA (master moves; this pins the cache per commit).
  resolve-libplacebo:
    runs-on: ubuntu-latest
    outputs:
      sha: ${{ steps.r.outputs.sha }}
    steps:
      - id: r
        run: |
          sha=$(git ls-remote https://code.videolan.org/videolan/libplacebo.git master | cut -f1)
          echo "libplacebo master HEAD = $sha"
          echo "sha=$sha" >> "$GITHUB_OUTPUT"

  build-linux:
    needs: resolve-libplacebo
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v5

      - name: Install build deps (mpv + FEL libs from source)
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            meson ninja-build pkg-config gcc nasm git patchelf zlib1g-dev \
            libass-dev liblua5.2-dev \
            libvulkan-dev libshaderc-dev glslang-dev liblcms2-dev \
            libva-dev libdrm-dev libegl-dev libgl-dev \
            libdvdnav-dev libdvdread-dev \
            libx11-dev libxss-dev libxext-dev libxpresent-dev libxrandr-dev \
            libwayland-dev wayland-protocols libxkbcommon-dev \
            libasound2-dev libpipewire-0.3-dev libpulse-dev \
            libopus-dev \
            libffi-dev libexpat1-dev \
            fontconfig libfreetype-dev libxml2-dev
          # NOTE: removed libbluray-dev — libbluray is rebuilt from source
          # with BD-J (libbluray.jar + JVM linkage) below.
          # x11: libx11-dev alone is NOT enough — mpv's meson x11 feature also
          # requires xscrnsaver/xext/xpresent/xrandr, and silently disables the
          # whole X11 vo when any of them is missing (issue #59: assets shipped
          # with no windowed vo and no audio ao at all).
          # wayland: client lib + protocols + xkbcommon for vo_gpu on Wayland.
          # audio: ALSA + PipeWire + PulseAudio aos.
          # fontconfig/freetype/libxml2-dev: deps for our libbluray rebuild
          # (BDJ jar build needs these present on PKG_CONFIG_PATH).

      - name: Setup JDK (for BD-J libbluray.jar + bundled jlink runtime)
        uses: actions/setup-java@v4
        with:
          distribution: ${{ env.JAVA_DISTRO }}
          java-version: ${{ env.JAVA_VERSION }}
          architecture: x64

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache FEL dep prefix
        id: felcache
        uses: actions/cache@v5
        with:
          path: fel-prefix
          key: fel-linux-${{ needs.resolve-libplacebo.outputs.sha }}-${{ hashFiles('deps-fel/**', 'scripts/build-fel-deps.sh') }}

      - name: Build FEL deps (libdovi + libplacebo master + ffmpeg master)
        if: steps.felcache.outputs.cache-hit != 'true'
        run: |
          PREFIX="$GITHUB_WORKSPACE/fel-prefix" WORK="$GITHUB_WORKSPACE/fel-work" \
            bash scripts/build-fel-deps.sh

      - name: Verify FEL deps
        run: |
          export PKG_CONFIG_PATH="$GITHUB_WORKSPACE/fel-prefix/lib/pkgconfig:$PKG_CONFIG_PATH"
          export PATH="$GITHUB_WORKSPACE/fel-prefix/bin:$PATH"
          # ffmpeg CLI must load OUR libavcodec, not a system one (the job installs
          # no libav*-dev), so put the prefix on the loader path.
          export LD_LIBRARY_PATH="$GITHUB_WORKSPACE/fel-prefix/lib:${LD_LIBRARY_PATH:-}"
          pkg-config --atleast-version=7.370 libplacebo
          echo "libplacebo $(pkg-config --modversion libplacebo)"
          ffmpeg -hide_banner -bsfs | grep -q dovi_split

      # Replaces the apt libbluray-dev (bdj_jar=disabled). Installs into the
      # same FEL prefix so our shadow PKG_CONFIG_PATH wins for the mpv build
      # and the bundler finds the .so. Uses the JDK set up above for
      # libbluray.jar (BD-J Java menus). Runs on every build (not cached) —
      # libbluray alone is tiny compared to FEL libs.
      - name: Build libbluray with BD-J (libbluray.jar + JVM linkage)
        run: |
          export PKG_CONFIG_PATH="$GITHUB_WORKSPACE/fel-prefix/lib/pkgconfig:${PKG_CONFIG_PATH:-}"
          export LD_LIBRARY_PATH="$GITHUB_WORKSPACE/fel-prefix/lib:${LD_LIBRARY_PATH:-}"
          echo "JAVA_HOME=$JAVA_HOME"
          PREFIX="$GITHUB_WORKSPACE/fel-prefix" \
          JAVA_HOME="$JAVA_HOME" \
            bash scripts/build-libbluray-bdj.sh

      # Build a slim jlink runtime (java.base only) and drop it into the FEL
      # prefix so the packaging step bundles it. libbluray dlopen's
      # $runtime/lib/server/libjvm.so; setting BLURAY_JVM_LIB_PATH at runtime
      # is also honoured. ~55 MB uncompressed, ~22 MB zipped.
      - name: Produce jlink jre (java.base) for Linux BD-J runtime
        run: |
          JR="$GITHUB_WORKSPACE/fel-prefix/share/libbluray/jre"
          jlink --add-modules java.base --output "$JR" \
                --strip-debug --no-man-pages --no-header-files --compress=2
          test -f "$JR/lib/server/libjvm.so" \
            || { echo "!! jlink output missing lib/server/libjvm.so" >&2; exit 1; }
          echo "jre size: $(du -sh "$JR" | cut -f1)"

      # mpv master needs wayland-client/-cursor/-scanner >= 1.23 (hard meson
      # requirement) and in practice wayland-protocols >= 1.44: its meson floor
      # says 1.38, but wayland_common.c references color-representation-v1,
      # whose XML only ships from protocols 1.44 — with older protocols the
      # header is not generated and the build dies on implicit declarations.
      # Noble ships 1.22 / 1.34. Build both into the FEL prefix
      # (PKG_CONFIG_PATH/PATH already point there for the mpv build, and the
      # bundler stages prefix libs). Runs on cache hits too — the pkg-config
      # guard makes it a no-op once the prefix has them.
      - name: Build wayland >= 1.23 into the FEL prefix
        run: |
          export PKG_CONFIG_PATH="$GITHUB_WORKSPACE/fel-prefix/lib/pkgconfig:$GITHUB_WORKSPACE/fel-prefix/share/pkgconfig:${PKG_CONFIG_PATH:-}"
          if pkg-config --atleast-version=1.23 wayland-client && \
             pkg-config --atleast-version=1.44 wayland-protocols; then
            echo "prefix already satisfies mpv's wayland requirements"; exit 0
          fi
          curl -fsSLO https://gitlab.freedesktop.org/wayland/wayland/-/archive/1.23.1/wayland-1.23.1.tar.gz
          tar xf wayland-1.23.1.tar.gz
          meson setup wayland-1.23.1/_b wayland-1.23.1 \
            --prefix="$GITHUB_WORKSPACE/fel-prefix" --libdir=lib --buildtype=release \
            -Ddocumentation=false -Dtests=false -Ddtd_validation=false
          meson install -C wayland-1.23.1/_b
          curl -fsSLO https://gitlab.freedesktop.org/wayland/wayland-protocols/-/archive/1.49/wayland-protocols-1.49.tar.gz
          tar xf wayland-protocols-1.49.tar.gz
          meson setup wayland-protocols-1.49/_b wayland-protocols-1.49 \
            --prefix="$GITHUB_WORKSPACE/fel-prefix" -Dtests=false
          meson install -C wayland-protocols-1.49/_b
          pkg-config --atleast-version=1.23 wayland-client
          pkg-config --atleast-version=1.44 wayland-protocols

      - name: Checkout Omniphony (renderer source)
        uses: actions/checkout@v5
        with:
          repository: mgth/Omniphony
          ref: ${{ env.OMNIPHONY_REF }}
          path: omniphony

      - name: Build liborender from source
        run: cargo build --release -p orender_ffi
        working-directory: omniphony/omniphony-renderer
        env:
          # This builds a *pinned external* source tree (Omniphony at
          # OMNIPHONY_REF), so lint policy there is not ours to enforce: an
          # immutable tag must stay buildable as rustc grows new lints.
          # dead_code in particular fires on liborender's dormant
          # artifact-evaluator path, which is reachable only through code the
          # FFI build never links.
          RUSTFLAGS: -D warnings -A dead_code

      - name: Install liborender into sysroot
        run: |
          mkdir -p sysroot/usr/lib sysroot/usr/include sysroot/usr/lib/pkgconfig
          rel=omniphony/omniphony-renderer/target/release
          cp "$rel/liborender.so" sysroot/usr/lib/liborender.so.0
          ln -sf liborender.so.0 sysroot/usr/lib/liborender.so
          cp omniphony/omniphony-renderer/orender_ffi/include/orender.h sysroot/usr/include/orender.h
          cat > sysroot/usr/lib/pkgconfig/orender.pc <<EOF
          prefix=${{ github.workspace }}/sysroot/usr
          libdir=\${prefix}/lib
          includedir=\${prefix}/include

          Name: orender
          Description: Omniphony renderer C library
          Version: 0.99.0
          Libs: -L\${libdir} -lorender
          Cflags: -I\${includedir}
          EOF

      - name: Apply orender patches onto mpv master
        run: |
          bash scripts/apply-patches-master.sh
          src="$(ls -d build/mpv-master-* | grep -v 'mpv-master-clone$' | head -n1)"
          echo "MPV_DIR=$src" >> "$GITHUB_ENV"

      - name: Build mpv (linked against FEL prefix)
        run: |
          PREFIX="$GITHUB_WORKSPACE/fel-prefix"
          # FEL prefix FIRST so its libplacebo/ffmpeg shadow the system ones.
          # share/pkgconfig: wayland-protocols installs its .pc there.
          export PKG_CONFIG_PATH="$PREFIX/lib/pkgconfig:$PREFIX/share/pkgconfig:${{ github.workspace }}/sysroot/usr/lib/pkgconfig"
          export LD_LIBRARY_PATH="$PREFIX/lib:${{ github.workspace }}/sysroot/usr/lib"
          export PATH="$PREFIX/bin:$PATH"
          cd "$MPV_DIR"
          # Hardware decoding forced on, all functional because the FEL ffmpeg
          # above is built with the matching support:
          #   cuda-hwaccel/-interop -> NVIDIA (ffnvcodec)
          #   vaapi                 -> AMD/Intel (VA-API)
          #   vulkan                -> Vulkan video decode (ffmpeg 8.1, vendor-neutral)
          # x11/wayland/alsa/pipewire/pulse forced on so a missing dev package
          # fails the configure instead of silently shipping a binary with no
          # desktop output backends (issue #59).
          meson setup _b -Dorender=enabled \
            -Dcuda-hwaccel=enabled -Dcuda-interop=enabled \
            -Dvaapi=auto -Dvulkan=enabled \
            -Dx11=enabled -Dwayland=enabled \
            -Dalsa=enabled -Dpipewire=enabled -Dpulse=enabled \
            -Dlibbluray=enabled -Ddvdnav=enabled
          meson compile -C _b
          echo "== mpv libplacebo linkage =="
          ldd _b/mpv | grep -E 'libplacebo|libavcodec' || true
          ldd _b/mpv | grep -q "$PREFIX/lib/libplacebo" \
            || { echo "!! mpv did not link the FEL libplacebo in $PREFIX" >&2; exit 1; }

      - name: Package (zip, bundling FEL libs + BD-J)
        run: |
          PREFIX="$GITHUB_WORKSPACE/fel-prefix"
          mkdir -p staging staging/lib staging/share/libbluray
          cp "$MPV_DIR/_b/mpv" staging/
          cp sysroot/usr/lib/liborender.so.0 staging/lib/
          cp sysroot/usr/include/orender.h staging/
          cp README.md staging/
          # FEL libs (libplacebo/ffmpeg/libdovi) + our rebuilt libbluray (so)
          cp -P "$PREFIX"/lib/lib{placebo,dovi,av*,sw*,bluray,udfread}*.so* staging/lib/ 2>/dev/null || true
          # BD-J: libbluray.jar next to where libbluray.so looks for it at
          # runtime (<prefix>/share/libbluray/libbluray.jar), and the jlinked
          # jre so users don't need a system JRE.
          if [ -f "$PREFIX/share/libbluray/libbluray.jar" ]; then
            cp "$PREFIX/share/libbluray/libbluray.jar" staging/share/libbluray/
            cp -R "$PREFIX/share/libbluray/jre" staging/share/libbluray/
          fi
          patchelf --set-rpath '$ORIGIN/lib' staging/mpv
          # Append a launcher README snippet (BD-J JVM hint)
          cat > staging/BD_J_RUNTIME.md <<'README_BDJ'
          # BD-J Blu-ray menus
          This bundle ships libbluray.jar and a jlinked java.base JRE under
          share/libbluray/. Set BLURAY_JVM_LIB_PATH at runtime if your libjvm
          lives elsewhere:
              export BLURAY_JVM_LIB_PATH="$PWD/share/libbluray/jre/lib/server/libjvm.so"
              ./mpv --disc-menu=yes --bluray-device=disc.iso bd://
          Most Disney/UHD discs use BD-J interactive menus; --disc-menu=yes is
          required to enter them (otherwise mpv plays the main title directly).
          README_BDJ
          cd staging
          zip -r "../mpv-omniphony-fel-linux-x86_64.zip" .

      - uses: actions/upload-artifact@v6
        with:
          name: mpv-omniphony-fel-linux-x86_64
          path: mpv-omniphony-fel-linux-x86_64.zip

  # macOS (Apple Silicon): native FEL build. Mirrors build-linux but builds the
  # FEL deps for arm64 (no NVIDIA; Vulkan via MoltenVK) and bundles every dylib
  # with otool/install_name_tool so the zip is self-contained.
  # EXPERIMENTAL: libplacebo's FEL reconstruction runs through MoltenVK, which is
  # only "nearly conformant" Vulkan on Apple Silicon — the first green run here is
  # the real proof it renders; visual confirmation is delegated to a tester.
  build-macos:
    needs: resolve-libplacebo
    runs-on: macos-14
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v5

      - name: Install build deps (mpv + FEL Vulkan stack)
        run: |
          brew update
          # We build our OWN libplacebo (master) + ffmpeg (master) from source,
          # so do NOT brew-install libplacebo/ffmpeg (they would clash on
          # PKG_CONFIG_PATH). molten-vk + vulkan-loader/-headers give the
          # Vulkan-on-Metal stack libplacebo's FEL path needs; cargo-c builds
          # libdovi (so build-fel-deps.sh skips its own `cargo install cargo-c`).
          # NOTE: removed `libbluray` — rebuilt from source with BD-J below.
          brew install meson ninja pkg-config cargo-c \
            libass luajit lcms2 shaderc glslang \
            molten-vk vulkan-headers vulkan-loader \
            libdvdnav libdvdread \
            opus fontconfig freetype libxml2

      - name: Setup JDK (for BD-J libbluray.jar compile + jlink runtime bundle)
        uses: actions/setup-java@v4
        with:
          distribution: ${{ env.JAVA_DISTRO }}
          java-version: ${{ env.JAVA_VERSION }}
          architecture: aarch64

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache FEL dep prefix
        id: felcache
        uses: actions/cache@v5
        with:
          path: fel-prefix
          key: fel-macos-${{ needs.resolve-libplacebo.outputs.sha }}-${{ hashFiles('deps-fel/**', 'scripts/build-fel-deps.sh') }}

      - name: Build FEL deps (libdovi + libplacebo master + ffmpeg master)
        if: steps.felcache.outputs.cache-hit != 'true'
        run: |
          # Make the brew Vulkan/shaderc/lcms visible so libplacebo's meson probes
          # resolve them; build-fel-deps.sh prepends its own prefix ahead of these.
          export PKG_CONFIG_PATH="$(brew --prefix)/lib/pkgconfig:$(brew --prefix vulkan-loader)/lib/pkgconfig:$(brew --prefix shaderc)/lib/pkgconfig:$(brew --prefix lcms2)/lib/pkgconfig:${PKG_CONFIG_PATH:-}"
          PREFIX="$GITHUB_WORKSPACE/fel-prefix" WORK="$GITHUB_WORKSPACE/fel-work" \
            bash scripts/build-fel-deps.sh

      - name: Verify FEL deps
        run: |
          export PKG_CONFIG_PATH="$GITHUB_WORKSPACE/fel-prefix/lib/pkgconfig:$(brew --prefix)/lib/pkgconfig:${PKG_CONFIG_PATH:-}"
          # ffmpeg CLI must load OUR libavcodec (the prefix), not a brew one.
          export DYLD_LIBRARY_PATH="$GITHUB_WORKSPACE/fel-prefix/lib:${DYLD_LIBRARY_PATH:-}"
          pkg-config --atleast-version=7.370 libplacebo
          echo "libplacebo $(pkg-config --modversion libplacebo)"
          "$GITHUB_WORKSPACE/fel-prefix/bin/ffmpeg" -hide_banner -bsfs | grep -q dovi_split

      # Replaces brew libbluray (bdj_jar=disabled, no JVM linkage). Installs into
      # the FEL prefix so the shadower PKG_CONFIG_PATH wins for the mpv meson
      # build and the dylib relocator later finds our libbluray. The setup-java
      # step above exposes JAVA_HOME for the jar build.
      - name: Build libbluray with BD-J (libbluray.jar + JVM linkage)
        run: |
          export PKG_CONFIG_PATH="$GITHUB_WORKSPACE/fel-prefix/lib/pkgconfig:$(brew --prefix)/lib/pkgconfig:${PKG_CONFIG_PATH:-}"
          export DYLD_LIBRARY_PATH="$GITHUB_WORKSPACE/fel-prefix/lib:${DYLD_LIBRARY_PATH:-}"
          echo "JAVA_HOME=$JAVA_HOME"
          # /usr/libexec/java_home on CI runners sometimes points at the system
          # VM; prefer the setup-java-installed JDK explicitly.
          PREFIX="$GITHUB_WORKSPACE/fel-prefix" \
          JAVA_HOME="$JAVA_HOME" \
            bash scripts/build-libbluray-bdj.sh

      # Bundle a jlinked JRE (java.base) so users don't need a system install.
      # macOS home: $JRE/Contents/Home/lib/server/libjvm.dylib (JDK-style).
      # The jre itself is packaged as a pseudo .app folder so macOS respects
      # quarantine / library loading; libbluray probes
      # $libbluray_jvm_lib_path -> $BLURAY_JVM_LIB_PATH -> JAVA_HOME/lib.
      - name: Produce jlink jre (java.base) for macOS BD-J runtime
        run: |
          JR="$GITHUB_WORKSPACE/fel-prefix/share/libbluray/jre"
          # jlink on macOS produces a JDK-style layout (no Contents wrapper
          # unless we build a full .app). Keep it flat so libbluray's dlopen
          # probe succeeds with BLURAY_JVM_LIB_PATH pointing at lib/server.
          jlink --add-modules java.base --output "$JR" \
                --strip-debug --no-man-pages --no-header-files --compress=2
          # jlink output layout on mac: lib/server/libjvm.dylib
          JVMLIB="$JR/lib/server/libjvm.dylib"
          [ -f "$JVMLIB" ] \
            || { echo "!! jlink output missing lib/server/libjvm.dylib (looked: $JVMLIB)" >&2; \
                 find "$JR" -name 'libjvm*' -type f; exit 1; }
          echo "jre size: $(du -sh "$JR" | cut -f1)"

      - name: Checkout Omniphony (renderer source)
        uses: actions/checkout@v5
        with:
          repository: mgth/Omniphony
          ref: ${{ env.OMNIPHONY_REF }}
          path: omniphony

      - name: Build liborender from source
        run: cargo build --release -p orender_ffi
        working-directory: omniphony/omniphony-renderer
        env:
          # This builds a *pinned external* source tree (Omniphony at
          # OMNIPHONY_REF), so lint policy there is not ours to enforce: an
          # immutable tag must stay buildable as rustc grows new lints.
          # dead_code in particular fires on liborender's dormant
          # artifact-evaluator path, which is reachable only through code the
          # FFI build never links.
          RUSTFLAGS: -D warnings -A dead_code

      - name: Install liborender into sysroot
        run: |
          mkdir -p sysroot/usr/lib sysroot/usr/include sysroot/usr/lib/pkgconfig
          rel=omniphony/omniphony-renderer/target/release
          cp "$rel/liborender.dylib" sysroot/usr/lib/liborender.dylib
          cp omniphony/omniphony-renderer/orender_ffi/include/orender.h sysroot/usr/include/orender.h
          cat > sysroot/usr/lib/pkgconfig/orender.pc <<EOF
          prefix=${{ github.workspace }}/sysroot/usr
          libdir=\${prefix}/lib
          includedir=\${prefix}/include

          Name: orender
          Description: Omniphony renderer C library
          Version: 0.99.0
          Libs: -L\${libdir} -lorender
          Cflags: -I\${includedir}
          EOF

      - name: Apply orender patches onto mpv master
        run: |
          bash scripts/apply-patches-master.sh
          src="$(ls -d build/mpv-master-* | grep -v 'mpv-master-clone$' | head -n1)"
          echo "MPV_DIR=$src" >> "$GITHUB_ENV"

      - name: Build mpv (linked against FEL prefix)
        run: |
          PREFIX="$GITHUB_WORKSPACE/fel-prefix"
          # FEL prefix FIRST so its libplacebo/ffmpeg shadow brew's; then orender;
          # then brew (incl. vulkan-loader for the GUI Vulkan/MoltenVK stack).
          export PKG_CONFIG_PATH="$PREFIX/lib/pkgconfig:${{ github.workspace }}/sysroot/usr/lib/pkgconfig:$(brew --prefix)/lib/pkgconfig:$(brew --prefix vulkan-loader)/lib/pkgconfig"
          export DYLD_LIBRARY_PATH="$PREFIX/lib:${{ github.workspace }}/sysroot/usr/lib"
          export PATH="$PREFIX/bin:$PATH"
          cd "$MPV_DIR"
          # mpv-player/mpv carries a non-v lightweight tag (`git-release`) that
          # `git describe --tags` resolves to, so mpv's generated version.h lacks
          # the leading `v` that master's osxbundle.py bundle_version() regex
          # requires — `re.findall(...)[0]` then dies with IndexError during
          # macos-bundle. Drop a v-tag at HEAD so describe yields a v-version
          # (it's distance 0, closer than git-release). FEL/master-only; the
          # v0.41.0 tarball builds already carry the v.
          ver=$(cat MPV_VERSION 2>/dev/null); git tag -f "v${ver%-UNKNOWN}-fel" HEAD >/dev/null 2>&1 || true
          # No CUDA/VA-API on macOS; Vulkan -> MoltenVK is the gpu-next path.
          # libbluray/dvdnav (BD/DVD/ISO) from brew. Force cocoa/swift/vulkan so the
          # GUI display stack is built; mpv's macos-bundle relocates everything next.
          meson setup _b -Dorender=enabled -Dvulkan=enabled \
            -Dlibbluray=enabled -Ddvdnav=enabled \
            -Dcocoa=enabled -Dswift-build=enabled
          meson compile -C _b
          echo "== mpv libplacebo linkage =="
          otool -L _b/mpv | grep -E 'libplacebo|libavcodec' || true
          otool -L _b/mpv | grep -q "$PREFIX/lib/libplacebo" \
            || { echo "!! mpv did not link the FEL libplacebo in $PREFIX" >&2; exit 1; }
          # Smoke: the binary + its whole dylib graph must load.
          _b/mpv --version
          _b/mpv --gpu-api=help | grep -qi vulkan \
            || echo "!! warning: vulkan gpu-api not listed (MoltenVK detection?)" >&2

      - name: Bundle into mpv.app (mpv's native macos-bundle)
        run: |
          # mpv's upstream osxbundle.py + dylib_unhell.py relocate every dylib
          # (liborender + the from-source FEL libplacebo/ffmpeg + brew deps) into
          # the app and wire MoltenVK + its ICD correctly — the same fix validated
          # for the stable release (#22/#37). FEL prefix first on PKG_CONFIG_PATH
          # so dylib_unhell's optional `pkg-config vulkan` probe resolves.
          PREFIX="$GITHUB_WORKSPACE/fel-prefix"
          export PKG_CONFIG_PATH="$PREFIX/lib/pkgconfig:${{ github.workspace }}/sysroot/usr/lib/pkgconfig:$(brew --prefix)/lib/pkgconfig:$(brew --prefix vulkan-loader)/lib/pkgconfig"
          cd "$MPV_DIR"
          # (version.h already carries a v-prefix — the build step tagged HEAD
          # with a v-version so `git describe` yields one; see that step's note.)
          meson compile -C _b macos-bundle   # -> _b/mpv.app

          # dylib_unhell relocates the Homebrew deps but leaves our from-source
          # FEL libplacebo/ffmpeg/libbluray (under $PREFIX/fel-prefix) referenced
          # by absolute path — they'd be missing on a clean Mac. Pull those (and
          # any transitive deps) into the bundle's lib dir, matching
          # dylib_unhell's layout (@executable_path/lib), with a small recursive
          # worklist. Runs before the Brand step's codesign + leak gate.
          APP=_b/mpv.app
          LIBDIR="$APP/Contents/MacOS/lib"; mkdir -p "$LIBDIR"
          worklist=("$APP/Contents/MacOS/mpv")
          for f in "$LIBDIR"/*.dylib; do [ -f "$f" ] && worklist+=("$f"); done
          while [ ${#worklist[@]} -gt 0 ]; do
            obj="${worklist[0]}"; worklist=("${worklist[@]:1}")
            while read -r ref; do
              base=$(basename "$ref")
              if [ ! -f "$LIBDIR/$base" ]; then
                real="$ref"; [ -f "$real" ] || real="$PREFIX/lib/$base"
                [ -f "$real" ] || { echo "  ! NOT FOUND: $ref" >&2; continue; }
                cp "$real" "$LIBDIR/$base"; chmod u+w "$LIBDIR/$base"
                install_name_tool -id "@executable_path/lib/$base" "$LIBDIR/$base"
                worklist+=("$LIBDIR/$base"); echo "  + relocated $base"
              fi
              install_name_tool -change "$ref" "@executable_path/lib/$base" "$obj" 2>/dev/null || true
            done < <(otool -L "$obj" | awk 'NR>1 {print $1}' | grep -E "^($PREFIX|/opt/homebrew|/usr/local)/")
          done

          # --- BD-J: libbluray.jar + jlinked JRE -----------------------------
          # libbluray dlopen's the JVM via a few env-var probes (BLURAY_JVM_HOME,
          # BLURAY_JVM_LIB_PATH, JAVA_HOME/lib). It also looks for libbluray.jar
          # relative to its own install prefix — since we relink libbluray.dylib
          # into @executable_path/lib we drop the jar + jre next to the
          # executable under a share/libbluray/ sibling and set JAVA_HOME via a
          # tiny launcher helper (the env vars are checked in-process; passing
          # them from a wrapper shim is the simplest portable fix).
          RES="$APP/Contents/Resources/libbluray"; mkdir -p "$RES"
          if [ -f "$PREFIX/share/libbluray/libbluray.jar" ]; then
            cp "$PREFIX/share/libbluray/libbluray.jar" "$RES/"
            echo "  + libbluray.jar ($(du -h "$RES/libbluray.jar" | cut -f1))"
          fi
          JRE_DST="$RES/jre"
          if [ -d "$PREFIX/share/libbluray/jre" ] && \
             find "$PREFIX/share/libbluray/jre" -name 'libjvm*' -type f | grep -q .; then
            cp -R "$PREFIX/share/libbluray/jre" "$JRE_DST"
            # Rewrite absolute install_names inside the jlinked JRE's dylibs.
            # jlink produces flat lib/ dylibs referenced by @rpath/libjli.dylib
            # — flattening them into @loader_path/. suffices for libbluray's
            # dlopen of libjvm.dylib (it's the leaf entry point).
            if [ -f "$JRE_DST/lib/server/libjvm.dylib" ]; then
              for d in "$JRE_DST/lib/"*.dylib "$JRE_DST/lib/server/"*.dylib; do
                [ -f "$d" ] || continue
                # make ids @loader_path-based and rewrite inter-references
                id="@loader_path/$(basename "$d")"
                install_name_tool -id "$id" "$d" 2>/dev/null || true
                for r in $(otool -L "$d" | awk 'NR>1{print $1}' | grep -E "^($PREFIX|/opt/homebrew|/usr/local|${JAVA_HOME:-/no})"); do
                  b="$(basename "$r")"
                  # libjli.dylib is a sibling in lib/
                  install_name_tool -change "$r" "@loader_path/$b" "$d" 2>/dev/null || true
                done
              done
              echo "  + jre (java.base, $(du -sh "$JRE_DST" | cut -f1))"
            else
              echo "  ! warning: jre missing lib/server/libjvm.dylib — skipping bundle-in" >&2
            fi
          fi
          # Drop a one-liner in Resources so we can confirm bundling later.
          [ -f "$RES/libbluray.jar" ] && ls -la "$RES/" | tee "$RES/.bundled_bdj"

      - name: Brand, verify and package the .app
        run: |
          cd "$MPV_DIR/_b"
          mv mpv.app mpv-omniphony.app
          APP=mpv-omniphony.app
          PB=/usr/libexec/PlistBuddy
          "$PB" -c "Set :CFBundleIdentifier fr.mgth.mpv-omniphony" "$APP/Contents/Info.plist"
          "$PB" -c "Set :CFBundleName mpv-omniphony"               "$APP/Contents/Info.plist"
          "$PB" -c "Add :CFBundleDisplayName string mpv-omniphony" "$APP/Contents/Info.plist" 2>/dev/null || true

          # --- BD-J launcher wrapper -----------------------------------------
          # Replace the mpv executable with a POSIX sh shim (checked into
          # scripts/mpv-bdj-launcher-macos.sh) that sets BLURAY_JVM_LIB_PATH /
          # BLURAY_CLASSPATH / JAVA_HOME before exec'ing the real binary.
          # The shim does NOT depend on working dir — uses dirname "$0".
          BIN="$APP/Contents/MacOS"
          if [ -f "$BIN/mpv" ] && [ ! -L "$BIN/mpv" ]; then
            mv "$BIN/mpv" "$BIN/mpv-bin"
            cp "${GITHUB_WORKSPACE}/scripts/mpv-bdj-launcher-macos.sh" "$BIN/mpv"
            chmod +x "$BIN/mpv"
            echo "  + BD-J launcher shim installed (wrapper mpv -> mpv-bin)"
          fi

          # BDJ presence assertions — fail if either the jar or the jvm ended up
          # missing (the user asked specifically for BD-J).
          test -f "$APP/Contents/Resources/libbluray/libbluray.jar" \
            || { echo "!! BD-J libbluray.jar not bundled into Resources/libbluray/" >&2; exit 1; }
          find "$APP/Contents/Resources/libbluray/jre" -name 'libjvm*' -type f | grep -q . \
            || { echo "!! BD-J jre bundle has no libjvm" >&2; find "$APP/Contents/Resources/libbluray/" || true; exit 1; }

          codesign --force --deep --sign - "$APP"
          "./$APP/Contents/MacOS/mpv-bin" --version
          # Smoke: the wrapper must also work (it's what Finder/Spotlight use
          # and what the CLI invokes).
          "./$APP/Contents/MacOS/mpv" --version >/dev/null \
            || { echo "!! wrapper launcher failed to execute" >&2; exit 1; }
          "./$APP/Contents/MacOS/mpv-bin" --gpu-api=help | grep -qi vulkan \
            || echo "!! warning: vulkan gpu-api not listed (MoltenVK detection?)" >&2
          test -f "$APP/Contents/Frameworks/libMoltenVK.dylib" \
            || { echo "!! libMoltenVK.dylib not bundled" >&2; exit 1; }
          test -f "$APP/Contents/Resources/vulkan/icd.d/MoltenVK_icd.json" \
            || { echo "!! MoltenVK ICD not bundled" >&2; exit 1; }
          # The bundled libMoltenVK keeps its brew install id by design (dlopen'd
          # via the ICD's relative path); exclude it. Catch any other absolute
          # brew/local OR un-relocated FEL-prefix dependency.
          leak=$(find "$APP" -type f \( -name '*.dylib' -o -name mpv-bin \) -print0 \
                 | xargs -0 -I{} sh -c 'otool -L "{}" | awk "NR>1{print \$1}"' \
                 | grep -E "^(/opt/homebrew|/usr/local|$GITHUB_WORKSPACE)" \
                 | grep -v '/libMoltenVK\.dylib$' | sort -u || true)
          [ -z "$leak" ] || { echo "!! un-bundled absolute references remain:" >&2; echo "$leak" >&2; exit 1; }
          zip -ry "${GITHUB_WORKSPACE}/mpv-omniphony-fel-macos-arm64.zip" "$APP"

      - uses: actions/upload-artifact@v6
        with:
          name: mpv-omniphony-fel-macos-arm64
          path: mpv-omniphony-fel-macos-arm64.zip

  build-liborender-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout Omniphony (renderer source)
        uses: actions/checkout@v5
        with:
          repository: mgth/Omniphony
          ref: ${{ env.OMNIPHONY_REF }}

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Build orender_ffi (release)
        run: cargo build --release -p orender_ffi
        working-directory: omniphony-renderer
        env:
          # This builds a *pinned external* source tree (Omniphony at
          # OMNIPHONY_REF), so lint policy there is not ours to enforce: an
          # immutable tag must stay buildable as rustc grows new lints.
          # dead_code in particular fires on liborender's dormant
          # artifact-evaluator path, which is reachable only through code the
          # FFI build never links.
          RUSTFLAGS: -D warnings -A dead_code

      - name: Stage artifact
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path out | Out-Null
          Copy-Item omniphony-renderer/target/release/orender.dll out/
          Copy-Item omniphony-renderer/orender_ffi/include/orender.h out/

      - uses: actions/upload-artifact@v6
        with:
          name: liborender-windows-from-source-fel
          path: out

  build-windows:
    needs: [resolve-libplacebo, build-liborender-windows]
    runs-on: ubuntu-latest
    container: archlinux:latest
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v5

      - name: Bootstrap Arch + Martchus mingw-w64 repo
        run: |
          pacman-key --init
          cat >> /etc/pacman.conf <<'EOF'

          [ownstuff]
          SigLevel = Optional TrustAll
          Server = https://martchus.no-ip.biz/repo/arch/$repo/os/$arch
          EOF
          pacman -Syu --noconfirm

      - name: Install MinGW toolchain + mpv/FEL build deps
        run: |
          pacman -S --noconfirm --needed \
            base-devel git unzip zip curl rustup nasm \
            mingw-w64-gcc \
            mingw-w64-pkg-config \
            mingw-w64-meson \
            mingw-w64-libass \
            mingw-w64-vulkan-headers mingw-w64-vulkan-icd-loader \
            mingw-w64-shaderc mingw-w64-spirv-cross mingw-w64-lcms2 mingw-w64-zlib \
            mingw-w64-libdvdnav mingw-w64-libdvdread \
            mingw-w64-opus \
            jdk-openjdk
          # ^ DVD playback (folder + ISO) from Martchus; libdvdnav -> libdvdread
          # read images natively. libbluray is built from source with embedded
          # libudfread + BD-J (jar) below. jdk-openjdk = HOST javac for the jar.
          # We build our OWN libplacebo (master) + ffmpeg (master) into the
          # MinGW sysroot below, so we intentionally do NOT install
          # mingw-w64-libplacebo / mingw-w64-ffmpeg here.
          rustup default stable
          rustup target add x86_64-pc-windows-gnu
          # Make JAVA_HOME point at the Arch openjdk install so the libbluray
          # meson build's `jdk_home` option resolves jni.h + javac.
          JH="$(dirname "$(dirname "$(readlink -f "$(command -v javac)")")")"
          echo "JAVA_HOME=$JH" >> "$GITHUB_ENV"

      - name: Install prebuilt cargo-c
        run: |
          # Use the prebuilt cargo-c binary instead of `cargo install cargo-c`:
          # building it from source drags in libz-sys/libnghttp2-sys HOST C builds
          # that fail to assemble in this container. (The Arch cargo-c package
          # would pull `rust`, which conflicts with rustup.) build-fel-deps.sh then
          # skips its own `cargo install` since cargo-cinstall is on PATH.
          CARGOC_VER=0.10.23
          curl -sL "https://github.com/lu-zero/cargo-c/releases/download/v${CARGOC_VER}/cargo-c-x86_64-unknown-linux-musl.tar.gz" \
            | tar -xz -C /usr/local/bin cargo-capi cargo-cbuild cargo-cinstall cargo-ctest
          command -v cargo-cinstall

      - name: Cache FEL dep prefix (MinGW sysroot libs)
        id: felcache
        uses: actions/cache@v5
        with:
          # Cache only the artefacts we install, not the whole sysroot.
          path: |
            /usr/x86_64-w64-mingw32/bin
            /usr/x86_64-w64-mingw32/lib/pkgconfig
            /usr/x86_64-w64-mingw32/lib/libplacebo*
            /usr/x86_64-w64-mingw32/lib/libav*
            /usr/x86_64-w64-mingw32/lib/libsw*
            /usr/x86_64-w64-mingw32/lib/libdovi*
            /usr/x86_64-w64-mingw32/include/libplacebo
            /usr/x86_64-w64-mingw32/include/libav*
            /usr/x86_64-w64-mingw32/include/libsw*
            /usr/x86_64-w64-mingw32/include/ffnvcodec
          # v2: previous key cached the libsw* libs + .pc but not their headers
          # (the include/libav* glob misses libswresample/libswscale), so a cache
          # hit left mpv unable to find <libswresample/swresample.h>. Bump to
          # rebuild + recache the prefix with those headers included.
          key: fel-mingw-v2-${{ needs.resolve-libplacebo.outputs.sha }}-${{ hashFiles('deps-fel/**', 'scripts/build-fel-deps.sh') }}

      - name: Write meson cross-file (x86_64-w64-mingw32)
        run: |
          mkdir -p sysroot
          cat > sysroot/mingw-cross.ini <<'EOF'
          [binaries]
          c = 'x86_64-w64-mingw32-gcc'
          cpp = 'x86_64-w64-mingw32-g++'
          ar = 'x86_64-w64-mingw32-ar'
          strip = 'x86_64-w64-mingw32-strip'
          pkg-config = 'x86_64-w64-mingw32-pkg-config'
          windres = 'x86_64-w64-mingw32-windres'
          # libplacebo (master HEAD) builds a Windows import lib and needs a
          # dlltool; meson only probes unprefixed llvm-dlltool/dlltool otherwise.
          dlltool = 'x86_64-w64-mingw32-dlltool'

          [host_machine]
          system = 'windows'
          cpu_family = 'x86_64'
          cpu = 'x86_64'
          endian = 'little'
          EOF

      - name: Build FEL deps into MinGW sysroot (cross)
        if: steps.felcache.outputs.cache-hit != 'true'
        run: |
          # PREFIX = the MinGW sysroot: our master libplacebo + ffmpeg shadow
          # Martchus's, so the cross pkg-config and the DLL-walker both pick ours
          # up with no further wiring.
          PREFIX=/usr/x86_64-w64-mingw32 \
          WORK="$GITHUB_WORKSPACE/fel-work" \
          CROSS_FILE="$GITHUB_WORKSPACE/sysroot/mingw-cross.ini" \
            bash scripts/build-fel-deps.sh --cross

      - name: Verify FEL deps (cross)
        run: |
          x86_64-w64-mingw32-pkg-config --atleast-version=7.370 libplacebo
          echo "libplacebo $(x86_64-w64-mingw32-pkg-config --modversion libplacebo)"
          x86_64-w64-mingw32-pkg-config --exists dovi

      - name: Build & install libsrt from source (ABI-matched to our libstdc++)
        run: |
          SYS=/usr/x86_64-w64-mingw32
          # --overwrite: the FEL ffmpeg-from-source build above already drops an
          # openssl (libcrypto/libssl) into $SYS, so a plain `pacman -S
          # mingw-w64-openssl` aborts with "conflicting files" on those unowned
          # files. Let pacman take ownership of them (ABI-compatible openssl 3.x).
          # (The stable Windows job doesn't hit this: it installs the prebuilt
          # mingw-w64-ffmpeg, which already owns openssl via its deps.)
          pacman -S --noconfirm --needed --overwrite '*' mingw-w64-cmake mingw-w64-openssl
          SRT_VER=1.5.5
          curl -sLO "https://github.com/Haivision/srt/archive/refs/tags/v${SRT_VER}.tar.gz"
          tar xzf "v${SRT_VER}.tar.gz"
          cd "srt-${SRT_VER}"
          mkdir build && cd build
          x86_64-w64-mingw32-cmake .. \
            -DENABLE_APPS=OFF -DENABLE_BONDING=OFF -DENABLE_STATIC=OFF \
            -DENABLE_SHARED=ON -DCMAKE_INSTALL_PREFIX="$SYS"
          cmake --build . -j"$(nproc)"
          cmake --install .
          if [ -f "$SYS/bin/srt.dll" ] && [ ! -f "$SYS/bin/libsrt.dll" ]; then
            cp "$SYS/bin/srt.dll" "$SYS/bin/libsrt.dll"
          fi

      - name: Build & install Lua 5.2 (MinGW)
        run: |
          SYS=/usr/x86_64-w64-mingw32
          LUA_VER=5.2.4
          curl -sLO "https://www.lua.org/ftp/lua-${LUA_VER}.tar.gz"
          tar xzf "lua-${LUA_VER}.tar.gz"
          cd "lua-${LUA_VER}"
          make PLAT=mingw CC=x86_64-w64-mingw32-gcc \
            AR="x86_64-w64-mingw32-ar rcu" RANLIB=x86_64-w64-mingw32-ranlib mingw
          install -Dm755 src/lua52.dll "$SYS/bin/lua52.dll"
          install -Dm644 src/liblua.a  "$SYS/lib/liblua52.a"
          for h in lua lualib lauxlib luaconf; do install -Dm644 "src/$h.h" "$SYS/include/$h.h"; done
          install -d "$SYS/lib/pkgconfig"
          cat > "$SYS/lib/pkgconfig/lua52.pc" <<EOF
          prefix=$SYS
          libdir=\${prefix}/lib
          includedir=\${prefix}/include

          Name: Lua
          Description: Lua 5.2 (MinGW cross build)
          Version: ${LUA_VER}
          Libs: -L\${libdir} -llua52
          Cflags: -I\${includedir}
          EOF

      - name: Build & install libbluray with BD-ISO (libudfread) + BD-J support
        run: |
          # Martchus ships libbluray without libudfread (BD folders only). Build
          # it from source with the vendored udfread embedded so raw BD ISOs
          # open; installs over the MinGW sysroot the mpv build below links to.
          # The cross-file was written above (Write meson cross-file step).
          # BDJ: host JDK installed above, passed in JAVA_HOME so the meson
          # build compiles libbluray.jar (cross-platform bytecode) using the
          # host javac, and the native glue links a host jni.h layout. The
          # resulting DLL looks for jvm.dll via %JAVA_HOME%\bin\server on the
          # user's Windows machine.
          echo "JAVA_HOME=$JAVA_HOME"
          CROSS_FILE="$GITHUB_WORKSPACE/sysroot/mingw-cross.ini" \
          JAVA_HOME="$JAVA_HOME" \
          BDJ_JAR=enabled \
            bash scripts/build-libbluray-mingw.sh

      - name: Download liborender (built from source)
        uses: actions/download-artifact@v7
        with:
          name: liborender-windows-from-source-fel
          path: /tmp/liborender

      - name: Install liborender into MinGW sysroot
        run: |
          SYS=/usr/x86_64-w64-mingw32
          install -Dm755 /tmp/liborender/orender.dll "$SYS/bin/orender.dll"
          install -Dm644 /tmp/liborender/orender.h   "$SYS/include/orender.h"
          install -Dm755 /tmp/liborender/orender.dll "$SYS/lib/orender.dll"
          install -d "$SYS/lib/pkgconfig"
          cat > "$SYS/lib/pkgconfig/orender.pc" <<EOF
          prefix=$SYS
          libdir=\${prefix}/lib
          includedir=\${prefix}/include

          Name: orender
          Description: Omniphony renderer C library
          Version: 0.99.0
          Libs: -L\${libdir} -lorender
          Cflags: -I\${includedir}
          EOF

      - name: Apply orender patches onto mpv master
        run: |
          bash scripts/apply-patches-master.sh
          src="$(ls -d build/mpv-master-* | grep -v 'mpv-master-clone$' | head -n1)"
          echo "MPV_DIR=$src" >> "$GITHUB_ENV"

      # ao_asio needs the Steinberg ASIO SDK headers (GPLv3 half of its dual
      # licence). Exports ASIO_SDK_DIR, consumed by -Dasio-sdk below.
      - name: Setup ASIO SDK
        uses: ./.github/actions/setup-asio-sdk

      - name: Cross-compile mpv (MinGW, linked against FEL sysroot)
        run: |
          cd "$MPV_DIR"
          # cuda-hwaccel forced on (FEL ffmpeg + nv-codec-headers provide nvdec);
          # cuda-interop left at auto for the MinGW GL/D3D interop path.
          meson setup _b \
            --cross-file="$GITHUB_WORKSPACE/sysroot/mingw-cross.ini" \
            -Dorender=enabled -Dlua=lua52 -Dasio=enabled -Dasio-sdk="$ASIO_SDK_DIR" -Dd3d11=enabled -Dcuda-hwaccel=enabled \
            -Dlibbluray=enabled -Ddvdnav=enabled
          meson compile -C _b

      - name: Package (zip, DLL-walker bundles FEL DLLs + BD-J from sysroot)
        shell: bash
        run: |
          SYS=/usr/x86_64-w64-mingw32
          mkdir -p staging staging/share/libbluray
          cp "$MPV_DIR/_b/mpv.exe" staging/
          cp "$MPV_DIR/_b/mpv.com" staging/
          cp "$SYS/bin/orender.dll" staging/
          cp "$SYS/include/orender.h" staging/
          cp README.md staging/
          if [ -f "$SYS/share/libbluray/libbluray.jar" ]; then
            cp "$SYS/share/libbluray/libbluray.jar" staging/share/libbluray/
            echo "  + libbluray.jar ($(du -h "$SYS/share/libbluray/libbluray.jar" | cut -f1))"
          else
            echo "!! libbluray.jar missing (BD-J not built); aborting per user spec." >&2
            exit 1
          fi
          # BD-J launcher + readme are checked into scripts/ to avoid the
          # YAML "literal scalar with bare heredoc delimiters at column 0" pit.
          cp "${GITHUB_WORKSPACE}/scripts/MPV-BD菜单启动.bat"   staging/MPV-BD菜单启动.bat
          cp "${GITHUB_WORKSPACE}/scripts/BD_J_运行说明.txt"     staging/BD_J_运行说明.txt
          echo "  + MPV-BD菜单启动.bat launcher + BD_J_运行说明.txt"
          should_skip() {
            case "$1" in
              KERNEL32.*|USER32.*|GDI32.*|ADVAPI32.*|MSVCRT.*|SHELL32.*|ole32.*|OLEAUT32.*|WS2_32.*|IMM32.*|VERSION.*|WINMM.*|d3d*|dxgi*|opengl32.*|setupapi.*|RPCRT4.*|bcrypt.*|CRYPT32.*|comdlg32.*|netapi32.*|powrprof.*|psapi.*|userenv.*|wininet.*|wintrust.*|comctl32.*|iphlpapi.*|secur32.*|uxtheme.*|dwmapi.*|crypt32.*|ncrypt.*|dnsapi.*|mpr.*|mswsock.*|odbc32.*|odbccp32.*|VCRUNTIME*|api-ms-*)
                return 0 ;;
            esac
            return 1
          }
          worklist=(staging/mpv.exe)
          while [ ${#worklist[@]} -gt 0 ]; do
            exe="${worklist[0]}"; worklist=("${worklist[@]:1}")
            for dll in $(x86_64-w64-mingw32-objdump -p "$exe" 2>/dev/null | awk '/DLL Name:/ {print $3}'); do
              should_skip "$dll" && continue
              [ -f "staging/$dll" ] && continue
              if [ -f "$SYS/bin/$dll" ]; then
                cp "$SYS/bin/$dll" staging/; worklist+=("staging/$dll"); echo "  + $dll"
              else
                echo "  ! NOT FOUND: $dll"
              fi
            done
          done
          for dll in libstdc++-6.dll libgcc_s_seh-1.dll libwinpthread-1.dll; do
            [ -f "$SYS/bin/$dll" ] && [ ! -f "staging/$dll" ] && cp "$SYS/bin/$dll" staging/ && echo "  + $dll (forced)"
          done
          ls -la staging/
          cd staging && zip -r "../mpv-omniphony-fel-windows-x86_64.zip" .

      - uses: actions/upload-artifact@v6
        with:
          name: mpv-omniphony-fel-windows-x86_64
          path: mpv-omniphony-fel-windows-x86_64.zip

  # Publish a pre-release only for a beta tag (e.g. v0.4.0-fel-beta.1), onto the
  # engine repo mgth/Omniphony (the download hub) as tag mpv-<ref>, like
  # release.yml. Skipped for cron/dispatch/path-push runs (those just upload CI
  # artifacts).
  publish-beta:
    if: startsWith(github.ref, 'refs/tags/')
    needs: [build-linux, build-macos, build-windows]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v7
        with:
          name: mpv-omniphony-fel-linux-x86_64
      - uses: actions/download-artifact@v7
        with:
          name: mpv-omniphony-fel-macos-arm64
      - uses: actions/download-artifact@v7
        with:
          name: mpv-omniphony-fel-windows-x86_64

      - name: Publish master beta pre-release
        uses: softprops/action-gh-release@v3
        with:
          # Publish to the engine repo (the download hub), like release.yml. The
          # 'mpv-' tag prefix matches neither Omniphony's v* (Studio) nor
          # liborender-v* triggers, so it starts no build there. Needs the
          # OMNIPHONY_RELEASE_TOKEN secret (contents:write on mgth/Omniphony).
          repository: mgth/Omniphony
          token: ${{ secrets.OMNIPHONY_RELEASE_TOKEN }}
          tag_name: mpv-${{ github.ref_name }}
          name: 'mpv-omniphony ${{ github.ref_name }} — master (spatial audio + enhanced video-range)'
          prerelease: true
          make_latest: false
          body: |
            Master-tracking build of mpv-omniphony: mpv **master** + the orender
            spatial-audio patches, with **native enhanced video-range playback**.
            liborender from Omniphony `${{ env.OMNIPHONY_REF }}`.
            **Beta — NOT a stable release.**

            **NOT bundled:** the decoder bridge plugin (`*_bridge.dll` / `.so`).
            Provide it via `--ad-orender-bridge-path=<absolute path>` or drop it
            next to `mpv(.exe)`.

            **macOS (Apple Silicon, experimental):** `…-fel-macos-arm64.zip` is
            self-contained (all dylibs bundled). Enhanced video-range rendering goes through
            Vulkan→MoltenVK, which is not yet fully conformant on Apple Silicon —
            treat it as experimental. Clear the download quarantine
            (`xattr -dr com.apple.quarantine mpv`), and point the Vulkan loader at
            the bundled MoltenVK ICD:
            `VK_ICD_FILENAMES=<dir>/share/vulkan/icd.d/MoltenVK_icd.json`.

            **Credits:** the enhanced video-range support is upstream mpv work by
            [kasper93](https://github.com/kasper93) (Kacper Michajłow) — mpv
            [PR #17932](https://github.com/mpv-player/mpv/pull/17932), merged into
            mpv master (v0.42.0), building on the matching libplacebo + ffmpeg
            upstream support. This build tracks upstream master and packages it.
          files: |
            mpv-omniphony-fel-linux-x86_64.zip
            mpv-omniphony-fel-macos-arm64.zip
            mpv-omniphony-fel-windows-x86_64.zip
